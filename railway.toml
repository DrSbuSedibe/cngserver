[build]
builder = "dockerfile"

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[[services]]
name = "cngserver"
rootDir = "server"

[services.variables]
NODE_ENV = "production"
PORT = "10000"